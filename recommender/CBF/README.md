vectorize_movies.py : 영화 데이터를 벡터화, DB에 저장

cbf.py : 벡터화된 정보를 가져와 유사도 계산. 유사도 0.2 보다 큰 영화 중 top 10개의 정보(ID와 유사도)를 서버에 보냄.

로컬DB 기준으로 코드를 짰기 때문에 추후 수정해야 함.

카테고리 선택 검색은 프론트에서 구현한 정보를 기준으로 쿼리문 작성하면 됨. 추후 카테고리로 필터링 된 영화들 중 유저 벡터화 정보를 기반으로 영화를 추천해주는 방식도 고려 중.
